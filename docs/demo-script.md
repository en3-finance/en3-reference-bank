# Demo Script

1. Open `mock/core-banking/users.json` and identify `user_001`.
2. Create or locate `wallet_001` in `mock/en3/wallets.json`.
3. Show the deposit address and incoming stablecoin deposit.
4. Submit the outgoing payment in `mock/en3/transactions.json`.
5. Explain that simulation and policy review require approval.
6. Approve the transaction using the control-plane concept.
7. Mark settlement in the transaction mock.
8. Review `mock/reconciliation/report.json`.
9. Review `mock/en3/audit-events.json`.
